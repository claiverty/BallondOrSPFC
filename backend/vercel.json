{
  "framework": null,
  "buildCommand": "cd .. && npm run build -w @awards/contracts && npm run build -w @awards/api",
  "functions": {
    "api/index.ts": { "includeFiles": "certs/**" }
  },
  "rewrites": [{ "source": "/api/:path*", "destination": "/api/index" }]
}
